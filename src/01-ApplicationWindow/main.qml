import QtQuick 6.6
import QtQuick.Controls 6.2

// Fenêtre principale
ApplicationWindow {
    //Propriétés héritées de Window
    visible: true

    height: 600
    width: 400
    minimumHeight : 550
    minimumWidth : 350
    maximumHeight : 650
    maximumWidth : 450

    color: "#B5BD00"
    title: "Window Text"
}
